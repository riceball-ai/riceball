# Docker Deployment

RiceBall recommends using Docker for deployment.

## Docker Compose

The project root directory provides a `docker-compose.yml` file which can be used directly.

```bash
docker-compose up -d
```
